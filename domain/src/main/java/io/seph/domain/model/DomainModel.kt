package io.seph.domain.model

/**
 * Represents the core data structure for the application's domain.
 *
 * This is a high-level interface and implementations should represent specific entities or
 * models within the domain layer.
 *
 * @interface DomainModel
 */
interface DomainModel